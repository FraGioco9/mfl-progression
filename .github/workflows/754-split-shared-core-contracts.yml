name: Issue 754 split Shared core contracts

on:
  push:
    branches:
      - arch/754-shared-core-contracts

permissions:
  contents: write

jobs:
  split:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: arch/754-shared-core-contracts
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Split canonical Shared core contracts
        run: |
          python .github/754-split-shared-core-contracts.py
          node --check site/modules/core-sources/shared-core-contracts.js
          node --check site/modules/core-sources/shared.js
          node --check site/modules/core-source-manifest.js
          node --check site/validate.mjs
          node --check site/validate-club-route-core.mjs
          node --check site/validate-core-source-ownership.mjs
          rm .github/754-split-shared-core-contracts.py
          rm .github/workflows/754-split-shared-core-contracts.yml
          git diff --check
      - name: Commit canonical split
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Split Shared core contract bridge"
          git push origin HEAD:arch/754-shared-core-contracts
